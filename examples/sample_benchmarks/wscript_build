#! /usr/bin/env python
# encoding: utf-8


bld.program(
    features = 'cxx',
    source   = bld.path.ant_glob('*.cpp'),
    target   = 'gauge_example',
    cxxflags = bld.toolchain_cxx_flags() + ['-std=gnu++0x'],
    use = ['gauge'])
